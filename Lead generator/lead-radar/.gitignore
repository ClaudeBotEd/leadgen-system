# Facebook scraper runtime data
data/fb_queue/
data/fb_state/

# Apify daily spend ledger — runtime state, regenerates per dag.
data/apify_spend.json

# Runtime logs (launchd, scrape runs, daily runs).
logs/
