# Lead Radar — Python dependencies
# Install: pip install -r requirements.txt
# Python 3.10+ vereist

# HTTP & scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
urllib3>=2.0.0

# Search engines (gratis, geen API key)
# ddgs is de hernoemde duckduckgo-search; raakt DDG direct ipv Bing-fallback
ddgs>=9.0.0
# Fallback voor wie nog op oude package zit — google.py probeert ddgs eerst
duckduckgo-search>=6.0.0

# Data & config
pyyaml>=6.0
pandas>=2.0.0

# DNS validation (voor email checks)
dnspython>=2.4.0

# Tekst processing
unidecode>=1.3.7

# Optioneel — voor mooiere CLI output
rich>=13.0.0

# Google Sheets sync (consumer/output/sheets.py)
gspread>=6.0.0
google-auth>=2.30.0

# LLM-verifier — Claude second-opinion op borderline scores
# Optioneel: zonder ANTHROPIC_API_KEY draait pipeline gewoon door (regex-only)
anthropic>=0.40.0

# Optioneel: error monitoring (auto-init via SENTRY_DSN env)
sentry-sdk>=2.0.0

# Optioneel: Reddit OAuth voor 10x rate-limit (toekomstig)
praw>=7.7.0

# Facebook scraper (consumer/sources/facebook/)
playwright>=1.48.0
tf-playwright-stealth>=1.1.0
pydantic>=2.5.0
pytest-asyncio>=0.23.0

# Apify cloud scrapers — primary FB groups collector.
# Self-hosted Playwright stack above remains as cold-storage fallback.
apify-client>=1.7.0
